{
  "name": "ir-cache",
  "version": "1.0.0",
  "private": true,
  "description": "Drop-in actions/cache replacement with IR S3 backend",
  "scripts": {
    "build": "tsc && ncc build -o dist/restore src/restore.ts && ncc build -o dist/save src/save.ts && ncc build -o dist/restore-only src/restoreOnly.ts && ncc build -o dist/save-only src/saveOnly.ts",
    "test": "jest --coverage",
    "lint": "tsc --noEmit"
  },
  "dependencies": {
    "@actions/cache": "^5.0.5",
    "@actions/core": "^2.0.3",
    "@actions/exec": "^2.0.0",
    "@actions/http-client": "^2.2.0",
    "@actions/io": "^2.0.0"
  },
  "devDependencies": {
    "@types/jest": "^29.5.0",
    "@types/node": "^22.0.0",
    "@vercel/ncc": "^0.38.0",
    "jest": "^29.7.0",
    "ts-jest": "^29.2.0",
    "typescript": "^5.5.0"
  }
}
